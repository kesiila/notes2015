######基本概念
 1.  正常流（`normal flow`）
 2.  非替换元素 
  	 * 如果元素内容包含在文档中，则称之为非替换元素
 3.  替换元素
   	 * 例如 <img></img>
 4.  块级元素
  	 * 这些元素在正常流中，会在其框之前和之后生成“换行”，所以处在正常流中的块级元素会垂直摆放。
 5.  行内元素
	* 块级元素的后代，不会在元素之前或者之后生成“行分隔符”
 6.  根元素
	* 文档书顶端的元素。在html文档中，就是html元素。在XML文档中，则可以是该语言允许的任何元素。
 
